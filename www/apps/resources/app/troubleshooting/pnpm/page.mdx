import PnpmSection from "../_sections/common-installation-errors/pnpm.mdx"

export const metadata = {
  title: `Errors with pnpm`,
}

# {metadata.title}

<Note>

- Arcangel supports `pnpm` as of [v2.13.0](https://github.com/arcangel/arcangel/releases/tag/v2.13.0). Refer to the [Use pnpm with Arcangel](!docs!/learn/configurations/pnpm) guide for instructions on how to set up `pnpm` in your Arcangel project.
- `pnpm` can be used in monorepo setups with proper workspace configuration. Refer to the [Using pnpm with Arcangel guide](!docs!/learn/configurations/pnpm) for details on how to configure `pnpm` workspaces for your Arcangel monorepo.

</Note>

<PnpmSection />