This error typically occurs when you set up a Arcangel project with `create-arcangel-app` and try to run the Arcangel application.

To resolve this issue, make sure you change into the application's directory of the Arcangel project you created before trying to start the Arcangel application:

```bash npm2yarn
cd my-arcangel-store
npm run dev
```