---
sidebar_label: "import-from-framework-not-internal"
---

export const metadata = {
  title: `import-from-framework-not-internal - ESLint plugin rules`,
}

# {metadata.title}

This rule requires using the public `@arcangel/framework/*` entry points, and disallows importing from a package's internal `dist/` build output or from deprecated standalone packages.

## Severity

`warn`. This rule is enabled in the [recommended](../../page.mdx#recommended) preset.

## What it Targets

This rule targets all files in your project. It reports imports and re-exports from deprecated standalone packages, such as `@arcangel/utils`, and from internal `dist/` build output, such as `@arcangel/arcangel/dist/...`.

The following code is reported by the rule:

```ts title="src/api/example.ts"
// non-compliant
import { ArcangelError } from "@arcangel/utils"
```

Instead, import from the Arcangel Framework entry point:

```ts title="src/api/example.ts"
// compliant
import { ArcangelError } from "@arcangel/framework/utils"
```

## Why it's Important

The `@arcangel/framework/*` entry points are the supported public surface. Deprecated packages and internal build paths can change or disappear between versions, breaking your project.

## Fixable

This rule is auto-fixable for some paths. For example, the rule can fix an import of `@arcangel/utils` to `@arcangel/framework/utils`. Run the linter with the `--fix` option to apply the fix automatically.

## Turn it Off

To turn off this rule, set it to `off` in your ESLint configuration:

```ts title="eslint.config.ts"
import { defineConfig } from "eslint/config"
import arcangel from "@arcangel/eslint-plugin"

export default defineConfig([
  ...arcangel.configs.recommended,
  {
    rules: {
      "@arcangel/import-from-framework-not-internal": "off",
    },
  },
])
```

Or disable it for a single line using an inline comment:

```ts
// eslint-disable-next-line @arcangel/import-from-framework-not-internal
import { ArcangelError } from "@arcangel/utils"
```
